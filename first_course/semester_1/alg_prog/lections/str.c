#include <stdio.h>

void naiveSearch(char *T, char *P)
{
    int n = strlen(T);
    int m = strlen(P);

    for (int i = 0; i <= n - m; i++)
    {
        int j;
        for (j = 0; j < m; j++)
        {
            
        }
    }
}

int main(void)
{
    char str[] = "artemkakin_lich_king";
    char search[] = "kin";

    return 0;
}